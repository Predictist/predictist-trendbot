## Predictist TrendBot (Data-only)

### 1) Setup
- Create database and run `app/models/schema.sql`.
- Copy `.env.example` → `.env` and set `DATABASE_URL`.

### 2) Install
```bash
pip install -r requirements.txt
